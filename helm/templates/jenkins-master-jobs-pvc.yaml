---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
 name: {{ printf "jenkins-master-job-pvc-%s" .Release.Name | trunc 63 }}
 annotations:
   volume.beta.kubernetes.io/storage-class: default
spec:
 accessModes:
  - ReadWriteMany
 resources:
   requests:
     storage: 1Gi
