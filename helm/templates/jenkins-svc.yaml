---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    jenkins: master
  ports:
    - name: http
      port: {{ .Values.service.internalPort }}
    - name: jnlp
      port: {{ .Values.service.jnlpPort }}